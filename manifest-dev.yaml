applications:
   - name: DevOpsExample
     random-route: true
     path: target/*.war
     memory: 512M
     instances: 1
     timeout: 180
     disk_quota: 1024M
     buildpack: java_buildpack_offline
